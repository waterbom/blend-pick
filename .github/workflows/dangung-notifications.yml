name: Dangung Reservation Notifications
on:
  schedule:
    - cron: '12,27,42,57 * * * *'
  workflow_dispatch:
concurrency:
  group: dangung-notifications
  cancel-in-progress: false
jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_SSH_KEY }}
          script: |
            set -e
            export NVM_DIR="$HOME/.nvm"
            [ -s "$NVM_DIR/nvm.sh" ] && . "$NVM_DIR/nvm.sh"
            cd ${{ secrets.EC2_APP_DIR }}
            node scripts/roll-dangung-calendar.cjs
            node scripts/retry-dangung-notifications.cjs
